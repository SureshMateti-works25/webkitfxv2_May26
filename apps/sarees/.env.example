# Optional. If unset in **dev**, the app calls `/api/...` and Vite proxies to http://127.0.0.1:5055.
# Set this to talk to the API directly (then CORS on Commerce.Api must allow your Vite port).
# VITE_COMMERCE_API_URL=http://localhost:5055
