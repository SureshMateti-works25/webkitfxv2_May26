# Groceries deploys to the Green Glacier SWA only (azure-static-web-apps-green-glacier-04e1d9b00.yml).
# This file stays so the repo matches any legacy Azure link; it does not push to Mango Bush.
# Remove the Mango Bush Static Web App in Azure if you do not need it, or extend this workflow if you assign it another app.
name: Azure SWA — mango-bush (idle)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Skipped
        run: echo "Groceries deploy uses workflow 'Azure SWA — Groceries (green-glacier)' only."
